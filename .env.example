# Local Docker Compose values only. Copy to .env and replace before shared use.
FRONTEND_PORT=4200
BACKEND_PORT=8080
MYSQL_PORT=3306
DB_NAME=url_shortener
DB_USERNAME=url_shortener
DB_PASSWORD=local-development-only
MYSQL_ROOT_PASSWORD=local-root-only
CACHE_ENABLED=false
RATE_LIMIT_ENABLED=true
